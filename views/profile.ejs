<!DOCTYPE html>
<html lang="ES-ES">
    <head>
        <meta charset="UTF-8">
        <link rel="stylesheet" href="/css/style.css"/>
        <link href="https://fonts.googleapis.com/css?family=Merriweather&display=swap" rel="stylesheet">
        <title>Profile</title>
    </head>
    <body>
        <%- include("header") %>

        <main>
            <h1>Your profile</h1>
            <div class="emptycontainer">
                <div class="profilecontainer">
                    <img class="profilepic" src="/<%= user.img %>"/>
                    <div class="profile">
                        <h2> <%= user.name %> </h2>
                        <div> <%= user.age %> </div>
                        <div> <%= user.gender %> </div>
                        <div> <%= user.points %></div>
                    </div>
                </div>

                <% if(owner) { %>
                    <div class="profilebutton"><a href="/updateProfile" class="button">Update profile</a></div>
                <% } %>
            </div>
        </main>
    </body>
</html>